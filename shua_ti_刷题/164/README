题目描述：给定一组数组，找出数组中超出数组数目一半的数字．

方法１: 这个数字超过了数组数目的一半，如果这个数组是有序的话，即是这个数组的中位数，所以我们可以借助与排序使这个数组有序，但是最快的排序方法也需要O(nlgn),所以这并不是一个好的方法，我们可以像快排一样，对数组进行partition,如果partition 的key值是数组的中位数，则partition结束，找到超过数组数目一半的数字．

方法２：traverse数组，用一个变量来存储当前数组中出现次数最多的数字，以及count来标记，如果当前数字和key相等，则count++,否则：如果count == 0,则key重新赋值为当前数字，count = 1;traverse结束后count 为1 , key即是要找的数字
